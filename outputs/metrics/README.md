# Metrics

Training and validation outputs (not the models themselves).

**Examples:** `random_forest.txt`, `xgboost.txt` — one line or short report per run (e.g. accuracy).

You can extend this to JSON with precision, recall, F1, or plots saved under a `figures/` subfolder if you add one later.
