# Notebooks — Random Forest

Put EDA, experiments, and reports for **Random Forest** only here (e.g. `eda.ipynb`, `evaluation.ipynb`).

Shared dataset: `data/raw/Train_Dataset.csv`. Training script: `models/random_forest/train.py`.
